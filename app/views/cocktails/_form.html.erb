<%= simple_form_for (@cocktail), id: "contact-form" do |f| %>
  <%= f.input :name, label: "What's the name of your cocktail ?", id: "your-name" %>
  <%= f.submit "add a cocktail", class: "btn btn-primary" %>
<% end %>
